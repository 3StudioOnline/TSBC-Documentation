---
sidebar_position: 9
title: Get Transaction By Hash
---
import {Step} from '@site/src/lib/utils.mdx'

## eth_getTransactionByHash

`eth_getTransactionByHash` Gets the information about a transaction requested by transaction hash. This function requires input as follows:

* URL <Step text="1"/> : RPC endpoint (Blockchain URL).
* ID <Step text="2"/> : RPC ID(Optional), used when calling multiple asynchronous RPC calls to differentiate your response data from each other.
* Hash <Step text="3"/> : Transaction hash.

![Eth Get Transaction By Hash](_static/img/eth_get-transaction-by-hash.png)

The returned *Response* <Step text="4"/> is a struct that holds the JSON- info of the header and body of the response as well as https status code.

:::note
If *Success* is *True* that only means that the response from the *Blockchain* was successful only.

If *Transaction Found* is *True* that only means that the *Response* was successful and the *Transaction Info* was found.
:::

If successful, returns the *Transaction Info* <Step text="6"/> which contains transaction data of the given hash.